# Machine authority: canonical port registry values
# Phase D (Ports Authority) — do not hardcode ports in docs/scripts.
# Use with docker compose via: `docker compose --env-file ops/ports.env ...`

# Database
POSTGRES_HOST_PORT=5433

# Cache
REDIS_HOST_PORT=6380

# RAG (Working KB query)
KB_QUERY_HOST_PORT=8011

# RAG (Decisions query)
DECISION_RAG_HOST_PORT=8021

