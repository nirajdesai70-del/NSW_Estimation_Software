[pytest]
markers =
    full: Tests that require FAISS, sentence-transformers, and full index rebuild
    order(n): execution order marker (used by repo tests)
addopts = --import-mode=importlib
testpaths = tests

